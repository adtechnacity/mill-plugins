import mill._, scalalib._

object `package` extends meta.MillBuildRootModule {
  val pluginVersion = sys.env("PLUGIN_VERSION")
  def mvnDeps = Seq(
    mvn"com.adtechnacity::mill-docs::$pluginVersion"
  )
}
